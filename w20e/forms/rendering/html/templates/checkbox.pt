<div metal:use-macro="load: control.pt">

  <input metal:fill-slot="input"
         type="checkbox" id="input-${control.id}" value="${value}"
         name="${control.id}" tal:attributes="checked string:${checked}"/>
</div>
