<fieldset id="${group.id}"
          class="flow ${group.orientation} ${fmtmap['extra_classes']}">
  <legend tal:condition="group.label">${group.label}</legend>
  <tal:replace replace="structure: content" />
</fieldset>
