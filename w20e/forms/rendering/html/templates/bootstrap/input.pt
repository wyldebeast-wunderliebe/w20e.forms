<div metal:use-macro="load: control.pt">

  <input
     metal:fill-slot="input"
     id="input-${control.id}" type="${fmtmap['input_type']}" name="${control.id}" 
     value="${value}" size="${control.cols}"
     placeholder="${fmtmap.get('placeholder','')}"/>
</div>
